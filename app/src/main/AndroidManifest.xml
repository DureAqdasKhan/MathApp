<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mathappt">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".OfflineFirebaseHandler"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Alarm" />
        <activity android:name=".CountingSong" />
        <activity android:name=".Calculator" />
        <activity android:name=".ChangePassword" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".BasicMathActivity" />
        <activity android:name=".DisplayTables" />
        <activity android:name=".Topics" />
        <activity android:name=".SignUp" />
        <activity android:name=".SignIn" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <receiver android:name=".PAlarm"
            android:enabled="true"
            android:exported="true"/>

        <service android:name=".MyService" />
    </application>

</manifest>